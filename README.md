# Discord Backup & Restore

OAuth system to verify users and save tokens for restore.

## Setup

1. Add environment variables:
   - CLIENT_ID
   - CLIENT_SECRET
   - REDIRECT_URI
   - BOT_TOKEN
   - GUILD_ID
2. Deploy on a Node.js host (Render, Railway, etc.)
3. Use /callback to verify users.
